{{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}

<div class="form-group row">
    {{ form_label(form.label, null, {'label_attr': {'class': 'col-sm-2 col-form-label'}}) }}
    <div class="col-sm-10">
        {{ form_widget(form.label, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.label) }}
    </div>
</div>

<button type="submit" class="btn btn-success" style="margin-top:20px ;margin-bottom:20px ">Submit</button>

{{ form_end(form) }}




